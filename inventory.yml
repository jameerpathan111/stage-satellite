all:
  vars:

    # For authentication with Satellite app
    satellite_username: admin
    satellite_password: changeme
    satellite_server_url: 'https://{{ ansible_fqdn }}'

  children:
    sat611:
      hosts:
        &sat611-stage :
    stage:
      hosts:
        #*sat610-stage :  # noqa colons
        *sat611-stage :  # noqa colons
      vars:
        rhsm_username: adstage
        rhsm_password: changeme
        ansible_user: root
        ansible_password: dog8code

